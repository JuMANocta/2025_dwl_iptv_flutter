<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <!-- §splashOff — Sur Android 12+, le système gère lui-même la fenêtre
             de lancement : on ne peut pas y peindre librement, on lui fournit un
             fond et une icône. L'icône est TRANSPARENTE pour ne rien montrer —
             sans ça le système retombe sur l'icône du launcher.
             Pour afficher le logo (§splashIdentity) : @drawable/android12splash,
             généré à 66 % pour survivre au masque circulaire du système.
             Durée d'animation 0 : Android impose de toute façon un minimum. -->
        <item name="android:windowSplashScreenBackground">@color/aether_boot_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_transparent</item>
        <item name="android:windowSplashScreenAnimationDuration">0</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.
         
         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- §splashHandoff — Fond explicite plutôt que `?android:colorBackground`,
             qui vaut BLANC sous Theme.Light : c'était une SECONDE source de
             flash, juste après le splash, et elle reste visible derrière
             l'UI Flutter. -->
        <item name="android:windowBackground">@color/aether_boot_background</item>
    </style>
</resources>
